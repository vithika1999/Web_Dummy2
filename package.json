{
  "name": "web_page_html",
  "version": "1.0.0",
  "description": "social guide",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "browser-sync start --server --files 'dist/css/*.css, **/*.html'"
  },
  "author": "vithi",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.27.7"
  }
}
